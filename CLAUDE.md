# CLAUDE.md

**Las instrucciones del proyecto viven en [`AGENTS.md`](AGENTS.md). Leelo primero.**

Este archivo solo existe para apuntar ahí y evitar duplicar contenido (dos fuentes que se
contradicen es peor que una). Todo lo importante —contexto, reglas de oro, dónde está la fuente
de verdad, cómo correr— está en `AGENTS.md` y en `docs/`.

## Notas específicas de Claude Code

- Hablar siempre en **español (castellano argentino)** con el usuario (Lucas).
- El registro de avance y el "dónde retomamos" está en [`ESTADO.md`](ESTADO.md).
- Entorno: Windows + PowerShell. `git` y `gh` instalados; `gh` logueado como `lucasmelchior-bit`.
  Node.js se instala en la Fase 1.
