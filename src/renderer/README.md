# src/renderer — Interfaz de usuario (HTML/CSS/JS)

Responsabilidad (se llena desde la **Fase 1**):

- Las pantallas de la app: productos, stock, pedidos de venta, importaciones, alertas, pronóstico.
- **UI en español**, con textos centralizados en un mapa `labels` (no literales sueltos).
- No accede directo a la base: pide datos vía la API expuesta por `preload` (IPC).

Ver [`../../docs/06-arquitectura.md`](../../docs/06-arquitectura.md) y
[`../../docs/07-convenciones.md`](../../docs/07-convenciones.md).
